Repo Manifest for the AAP Toolchain
===================================

To clone the toolchain repositories:

```
mkdir aap-toolchain
cd aap-toolchain
repo init -u https://github.com/embecosm/aap-manifest.git [-b branch]
repo sync
```
